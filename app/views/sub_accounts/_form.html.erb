<%= form_for(@sub_account) do |f| %>
  <% if @sub_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_account.errors.count, "error") %> prohibited this sub_account from being saved:</h2>
      <ul>
      <% @sub_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>	
	<%= f.hidden_field :account_id, :value => @account.id %>
  <div class="actions">
    <%= f.submit "Add Account", :class => "btn btn-primary" %>
  </div>
<% end %>
